name: dependabot-sync

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "dependabot-sync is intentionally disabled in this fork"
